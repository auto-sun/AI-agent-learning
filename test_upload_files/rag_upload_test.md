# RAG Markdown Upload Test

This markdown file is used to test the unified document parser.

- File type: Markdown
- Expected parser: extract_text_from_md
- Expected upload route: /rag/upload-and-add

The RAG system should save the file, parse the text once, and add the parsed content to the vector store.
